apiVersion: v1
kind: Service
metadata:
  name: {{ include "infra-maps.fullname" . }}
  labels:
    {{- include "infra-maps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infra-maps.selectorLabels" . | nindent 4 }}
